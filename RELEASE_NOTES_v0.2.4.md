# clash-yaml-builder v0.2.4

## Highlights

- Make the final-step copy-link export flow visible for both Sparkle and Clash targets.
- Rename the updatable export action from "subscription URL/address" to "subscription link" for clearer beginner-facing wording.
- Keep the copy-link export button visible in the last step and disable it only when no subscription link has been added yet.
- Add a blog guide SVG asset under `public/` for project content and documentation usage.

## Notes

- `Export YAML` remains the backup/manual import path.
- `Copy ... subscription link (updatable)` is the recommended daily-use path once at least one subscription link has been added.
